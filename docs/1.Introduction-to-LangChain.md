# 📘 Prompts in LangChain

## 🔹 What are Prompts?
**Prompts** are the **input instructions or queries** given to a Large Language Model (LLM) to guide its output.  

The **quality, clarity, and structure** of a prompt directly affect the model’s performance.  
Because of this, a specialized role called **Prompt Engineer** exists, focusing on designing effective prompts.

---

## 🔹 Why are Prompts Important?
1. **Control Output Quality** – A well-crafted prompt can make responses more accurate, relevant, and structured.  
2. **Reduce Ambiguity** – Prompts help the model understand *exactly what is required*.  
3. **Enable Complex Tasks** – Multi-step reasoning, summarization, and formatting can all be influenced by how prompts are framed.  

---

## 🔹 PromptTemplate in LangChain
A **PromptTemplate** is a structured way to create prompts dynamically by inserting variables into a predefined template.  

Instead of hardcoding, you define placeholders (`{variable}`) that can be **filled at runtime** with different inputs.  

✅ **Reusable** – Define once, reuse across multiple tasks.  
✅ **Flexible** – Adjusts automatically to new inputs.  
✅ **Manageable** – Easier to maintain in complex workflows.  

---

## 🔹 Why use PromptTemplate over f-strings?
1. **Default Validation** – Ensures required variables are present.  
2. **Reusability** – Templates can be stored, shared, and versioned.  
3. **Ecosystem Integration** – Works smoothly with other LangChain components (Chains, Agents, Runnables).  

---

## 🔹 Example: Using PromptTemplate
```python
from langchain.prompts import PromptTemplate

# Define template
template = "Translate the following text into {language}:\n\n{text}"

# Create PromptTemplate
prompt = PromptTemplate(
    input_variables=["language", "text"],
    template=template,
)

# Format with dynamic inputs
final_prompt = prompt.format(language="French", text="Hello, how are you?")
print(final_prompt)
